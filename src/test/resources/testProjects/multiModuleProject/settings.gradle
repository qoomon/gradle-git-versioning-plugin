pluginManagement {
    repositories {
        mavenLocal()
        gradlePluginPortal()
    }
}

rootProject.name = 'gradle-git-versioning-plugin-test-multi-module-project'

include 'api'
include 'logic'
